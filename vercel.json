{
  "buildCommand": "cd offer-letter-genrator && npm install && npm run build",
  "outputDirectory": "offer-letter-genrator/build",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
