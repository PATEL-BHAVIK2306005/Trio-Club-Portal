{
  "buildCommand": "cd offer-letter-genrator && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "offer-letter-genrator/build",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
